import { createTheme } from "@mui/material/styles";

const Theme = createTheme({
  palette: {
    pupMaroon: {
      main: "#880000",
      contrastText: "#ffffff",
    },
    pupGold: {
      main: "#ffd700",
      contrastText: "#000000",
    },
  },
});

export default Theme;
